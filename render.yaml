services:
  - type: web
    name: ai-travel-planner
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --workers=2 --timeout 120 --max-requests=100 --max-requests-jitter=20 app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
